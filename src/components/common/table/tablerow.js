import React, {Component} from 'react';

export default class Tr extends Component{
    constructor(props) {
        super(props);
    }

    render() {
        return (
            // this.props.row.map()
        )
    }
}